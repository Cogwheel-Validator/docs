[project]
name = "mdx-gen"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiohttp>=3.14.1",
    "httpx[http2]>=0.28.1",
    "jinja2>=3.1.6",
    "pyyaml>=6.0.3",
]

[tool.ruff.lint]
select = ["ALL"]
ignore = ["F401"]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]

[tool.uv]
exclude-newer = "7 days"
